# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'i-partner-test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for i-partner-test
  pod 'Alamofire'
  pod 'SwiftyJSON'

  target 'i-partner-testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'i-partner-testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
